{
  "name": "@trademate/extension",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite build --watch",
    "build": "vue-tsc -p tsconfig.json --noEmit && vite build",
    "lint": "vue-tsc -p tsconfig.json --noEmit",
    "typecheck": "vue-tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@trademate/shared-types": "workspace:*",
    "vue": "^3.5.13"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.321",
    "@vitejs/plugin-vue": "^5.2.3",
    "typescript": "^5.8.3",
    "vite": "^6.3.5",
    "vue-tsc": "^2.2.10"
  }
}
