# Browser Helpers

Browser fallback actions and contract are implemented in:

1. `action_contract.js` (validate / prepare / execute / verify runtime contract)
2. `actions.js` (pause_campaign, resume_campaign, add_negative_keyword, pause_keyword)
